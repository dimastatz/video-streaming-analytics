package dimastatz.flumenz.tests

import org.scalatest.funsuite.AnyFunSuite

class CdnPerformanceTests extends AnyFunSuite with SparkTest {
  test(testName = "testLoadInput") {
    assert(1 == 1)
  }
}
